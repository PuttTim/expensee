import 'package:flutter/material.dart';

class BillFormScreen extends StatelessWidget {
  const BillFormScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
